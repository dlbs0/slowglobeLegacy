{
  "scripts": {
    "dev": "npm run dev --workspace=engine",
    "build": "npm run build --workspace=engine",
    "lint": "npm run lint --workspaces",
    "create-trip": "npm run create-trip --workspace=engine",
    "encode-video": "npm run encode-video --workspace=engine",
    "convert-images": "npm run convert-images --workspace=engine",
    "postinstall": "patch-package"
  },
  "workspaces": [
    "trips",
    "engine"
  ],
  "devDependencies": {
    "@rushstack/eslint-patch": "^1.11.0",
    "@vue/eslint-config-prettier": "^9.0.0",
    "@vue/eslint-config-typescript": "^13.0.0",
    "eslint": "^8.57.1",
    "eslint-plugin-vue": "^9.33.0",
    "patch-package": "^8.0.0",
    "prettier": "^3.5.3"
  }
}
